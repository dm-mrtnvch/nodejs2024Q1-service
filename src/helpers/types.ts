export enum Entities {
  User = 'User',
  Artist = 'Artist',
  Track = 'Track',
  Album = 'Album',
}

export enum Favorites {
  Tracks = 'tracks',
  Albums = 'albums',
  Artists = 'artists'
}

export enum FavoritesIds {
  TrackId = 'trackId',
  AlbumId = 'albumId',
  ArtistId = 'artistId'
}
